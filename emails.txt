Thread 1 - From: boss@company.com – Subject: “Urgent: Client escalation needs response today”
Thread 2 - From: newsletter@medium.com – Subject: “10articles you might like this week”
Thread 3 - From: teammate@company.com – Subject: “Quick question about the Q3 deck”
Thread 4 - From: billing@software.com – Subject: “Your invoice is ready”
Thread 5 - From: recruiter@linkedin.com – Subject: “Exciting opportunity at the fast growing  startup”

--------------------------------------------------------------------------------------------------------------

Update app.py to add the Approval Gate and Export Proof phases:
APPROVAL GATE: 1.Loop through drafts that haven’t approved/rejected yet 
2.For each: show thread subject,two columns(left=full thread, right=editable text_area with draft)
3.Three buttons:Approve(saves edited text to st.session_state.approved),Regenerate(calls draft_reply again), Reject(adds to rejected set)
4.Show running count: X approved, Y rejected
5.When all reviewed:st.balloons() + ‘Go to Export Proof’ EXPORT PROOF:
1 show preview of all approved  drafts side-by-side
2.Generate_proof_markdown() function:creates markdown with title,date, for each approved thread shows original messages quoted + draft in code block
3.generate_proof_html() function:styled dark-theme HTML with Css grid()original left with orange border,draft right with grren border)
4.Two download buttons:Download Proof (Markdown), Download Proof(HTML)
5.Note: ‘Share with #MyChiefOfStaff to earn your Ghostwriter badge!’
This reuses the same approve/edit/reject pattern from earlier’s approval_gate.py.


#  def _build_gmail_service():
#      from google.auth.transport.requests import Request 
#      from google.oauth2.credentials import Credentials
#      from googleapiclient.discovery import build
#     #  from googleapiclient.errors import HttpError      
#      from google_auth_oauthlib.flow import InstalledAppFlow
     
#      here = os.path.dirname(os.path.abspath(__file__))
#      creds_path = os.path.join(here, "credentials.json")
#      token_path = os.path.join(here, "token.json")
     
#      print(f"[DEBUG] Credentials path: {creds_path}")
#      print(f"[DEBUG] token path = {token_path}")
     
#     scopes=['https://www.googleapis.com/auth/gmail.readonly'
#             'https://www.googleapis.com/auth/gmail.send']
    
#     creds: Credentials | None = None
#     if os.path.exists(token_path):
#         print("[DEBUG] token.json exists")
#         try:
#             creds = Credentials.from_authorized_user_file(token_path, scopes)
#         except ValueError as e:
#             print(f"[DEBUG] token invalid {e}")
#             creds == None


